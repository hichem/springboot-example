apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-user.fullname" . }}
  labels:
{{ include "app-user.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "app-user.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
